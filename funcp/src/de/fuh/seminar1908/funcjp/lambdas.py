# Additionsfunktion
function = lambda x, y: x + y

# Produziert die Zahl 10
supplier = lambda: 10

# Gibt ein Objekt auf der Standardausgabe aus
consumer = lambda o: print(o)

# Ein Prädikat für gerade Zahlen
predicate = lambda x: x % 2 == 0